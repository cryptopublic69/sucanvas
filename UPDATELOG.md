# UPDATELOG

## 2026-08-10

### 工作流方案管理

- 新增本机工作流方案仓库，每套方案独立保存 `manifest.json`、API 格式 `workflow.json`、`adapter.json`、`ui-schema.json` 和参数默认值；首次使用时会从现有 H3 工作流创建默认的“MiniMax H3 全能参考”方案。
- 应用设置新增“工作流方案”类目，可按功能与视频子类型管理多套方案，设置各类型默认方案，并为生成节点单独选择具体方案与修订版本；当前 v1 通用执行引擎正式支持多参生视频、首尾帧、图生视频和尾帧生视频，文生视频与图片生成保留分类入口但暂不开放创建。
- 新建、另存和覆盖方案前会校验 API 工作流格式、输入契约、关键节点、LoRA 加载器类型及高级节点映射；覆盖已有方案时自动建立包含工作流、适配器和界面定义的恢复点。
- 支持将方案导出为 ZIP 包、从 ZIP 导入为新方案、用 ZIP 完整恢复当前方案，以及恢复到最近一次覆盖前状态；文件选择使用原生系统对话框。
- 方案删除改为回收站流程，可恢复或永久清除；删除仍被节点或生成快照引用的方案时，可先批量替换为同类型方案，也可保留缺失状态以阻止误用。
- 生成与二采不再依赖写死的 H3 节点 ID，而是读取所选方案的适配器、素材数量契约、LoRA 目录和节点映射；生成快照持久化方案 ID 与修订版本，方案缺失时阻止继续生成或二采。
- 仓库新增 `workflows/MiniMax_H3_全能参考-20260809-140350-6c523fc51b6f4f89813a252b215055cb.zip` 完整方案包，内含 manifest、与当前仓库工作流一致的 workflow、适配器和参数界面定义。
- 新增 `MiniMax+H3首尾帧工作流.json`、`MiniMax+H3图生视频工作流.json` 和 `MiniMax+H3尾帧生视频工作流.json`，分别使用 FL2VA、I2VA 与 L2VA 任务类型；内置适配器为三种路由提供独立输入契约和首尾帧节点映射。
- 工作流校验会递归检查所有节点连接，提交到 ComfyUI 前即可发现数字节点 ID 或指向不存在节点的连接；首尾帧必须恰好两张图片，图生/尾帧生视频必须恰好一张图片，三种模式均拒绝音频和视频参考。

### H3 参数与 LoRA

- 应用设置新增“模型参数”类目，按工作流方案保存一采 Audio Steps，以及一采、二采各自的亮度、对比度和饱和度；Video Steps 与二采调度 Steps 移至每个视频生成节点，支持滑块旁直接输入整数。
- 新增 MiniMax H3 基础模型选择，从 ComfyUI `models/diffusion_models/MinimaxH3` 目录动态读取可用模型，随工作流方案保存并在生成、二采及恢复前校验；默认工作流适配器新增 UNET 加载器与二采 Guider 映射。
- 一采与二采 LoRA 改为完全独立的模型、权重和启用开关；二采可单独不选模型并保持关闭，后端分别连接一采采样器、二采调度器与 Guider，避免两个阶段互相覆盖。
- LoRA 权重支持滑块和数值框双向编辑，一采/二采 Steps 也可在节点内逐项调整；选择、旁路状态和参数会进入生成快照、任务持久化、断线恢复和生成信息面板。
- LoRA 列表按当前方案声明的加载器类型和模型目录动态读取；目录刷新后分别修正一采与二采的失效选择，未旁路且模型缺失时阻止对应阶段执行并显示具体错误。
- H3 输出文件名前缀改为运行时日期模板 `%date:yyyy-MM-dd%/Minimax_H3`，提交前由后端解析为当天目录，不再需要每天手动修改工作流 JSON。
- 多参生视频新增参考图尺寸 `max` / `match` 切换，并将选择写入生成快照、重新生成与二采参数；首尾帧图片会按显式 `first` / `last` 角色映射到对应加载器，不再依赖连接顺序。

### 完整备份与数据迁移

- 应用设置新增“数据备份”，可将数据库一致性快照、已导入素材、工作流方案与适配器、方案恢复点、应用锁和 `infinite-canvas:` 软件设置导出为单个 `.sucanvas-backup` 文件。
- 恢复流程会先校验备份格式、路径、文件数量、解压大小和 SQLite 完整性，再写入待恢复目录；下次启动时原子替换数据目录，并保留恢复前的完整目录用于回退。
- 软件数据目录统一迁移到 `D:\Data\SuCanvasData\data`；首次启动会从旧 `%LOCALAPPDATA%\InfiniteCanvas` 完整复制数据库、素材和工作流等数据，并递归重写数据库中的旧素材绝对路径，旧目录继续保留。
- 备份明确排除实时 `api.json` 和 ComfyUI 外部输出目录；恢复完成后重新生成本机 API 配置，并自动导入备份中的前端设置。

### 预览节点与画布交互

- 生成视频预览改为横向 420 像素、竖向 300 像素的自适应宽度，并以 38 像素专用底栏替代原节点标题栏；旧预览会按保存的真实比例自动迁移到新布局。
- 视频预览底栏集中二采/取消、颜色、定位、生成信息和删除操作；新增生成信息面板，显示 Seed、时长、比例、耗时、LoRA 与旁路状态、一采/二采参数及提示词，并提供 Seed 和提示词复制。
- 导入视频和生成视频改为悬停即播放、移出即暂停，生成视频底栏新增全屏播放；全屏退出后仅在鼠标仍位于视频上方时继续播放，预览画面统一铺满节点区域。
- 图片节点按自然比例使用更紧凑的 38 像素底栏并支持等比缩放；导入时立即读取原始尺寸，竖图自动采用较窄宽度，图片和音频删除入口移至底栏，普通节点删除不再隐式删除应用资产文件。
- 画布默认支持直接拖拽框选并采用部分相交选择，Ctrl 用于追加多选；新增鼠标中键拖动画布并屏蔽浏览器自动滚屏，空白画布默认光标不再持续显示抓手，节点搜索栏暂时隐藏。
- 调整普通选中、二采色、预设色和关联高亮的描边宽度，避免多层边框造成节点尺寸和内容区域视觉抖动。
- 节点拖动由固定网格吸附改为可视区域内的边缘对齐与等距吸附，多选拖动同样生效，并实时显示对齐虚线和间距标尺；最终吸附位置统一持久化。
- 生成预览的自动落位会沿同列跳过已有节点；占位节点为每个任务生成稳定但不同的光团位置和运动轨迹，并以底部细进度条及百分比展示实时状态。
- 应用重启时可直接从画布中的持久化占位节点重建缺失任务记录；失败或取消状态会先写回数据库再清理任务，写回失败时保留恢复记录，降低任务结果丢失风险。
- 未连接的节点输出端口默认隐藏，在悬停、选中或开始连线时显示；媒体节点缩放控件精简为四个可见角点，选中光晕和自定义颜色在深浅主题下保持一致。
- 支持 `Ctrl+Delete` 或按住 Ctrl 点击生成视频删除按钮，跳过二次选择并直接删除所选生成视频节点及其源文件；源文件已不存在时删除仍可幂等完成。
- 一采视频新增“重新生成”：普通点击使用历史提示词、素材、模型与参数并生成新 Seed，Ctrl+点击可在弹窗中调整 Seed、分辨率、LoRA 强度、Steps、画面参数和参考图模式；新预览固定排列在来源视频下方。
- 二采按钮支持 Ctrl+点击打开参数弹窗，可调整 Seed、分辨率、LoRA 开关与强度、Scheduler Steps、画面参数和参考图模式；普通点击继续沿用快捷二采。
- 视频底栏新增定位按钮，点击可回到生成时捕获的提示词节点，Ctrl+点击可定位关联的视频生成节点；生成信息面板同步显示提示词标题、版本名、参考图模式和目标视频时长。
- `NEW` 标记改为视频完整播放结束后才移除，短暂开始播放或悬停预览不会提前清除未看完状态。

### 提示词与节点编排

- 新增“提示词版本节点”，可在同一节点保留 v1、v2、v3 等历史内容，切换当前生成版本、复制当前内容创建新版本，并将任一版本标记为最佳；生成快照固定记录版本 ID、版本名和提示词节点标题。
- 视频生成节点可同时连接多个文字节点，点击任一文本行切换当前用于生成的提示词；列表支持拖拽或键盘调整顺序，并可在确认后一次断开全部文本连接而不删除原节点。
- 多选文字节点后发起连接，会按中文标题自然升序批量接入视频节点并保留顺序；点击已连接文字节点会同步切换视频节点的当前提示词和关联高亮。
- 视频生成节点高度随素材和文字数量增长，最多直接显示十行文字并支持底部手动收起；新增、粘贴、导入及并发接收的节点统一预留防重叠位置。

### 界面与性能

- 基础设置新增“小/中”两档界面字号，中字号同步扩大文字、控件、菜单和弹窗间距，并持久化到本机及完整备份。
- 节点渲染增加可见区域裁剪、组件记忆化和输入/输出派生数据缓存，画布移动、搜索与任务进度更新时减少无关节点重复渲染。
- 视频节点下拉菜单新增展开动画和箭头状态，LoRA 控件改为紧凑开关与数值输入；项目搜索、生成信息滚动、上下文菜单顺序及亮暗主题细节同步调整。
- 启用全新的 SuCanvas 无限画布与播放图标，替换首页、应用锁、窗口和安装包图标；仓库保留设计母版、方案稿及 Windows/macOS/Android/iOS 全尺寸资源，构建脚本会在 Windows 图标变化时重新嵌入资源。
- 首屏在 React 加载前读取已保存主题并设置窗口背景，避免浅色主题启动时闪黑或深色主题闪白；项目首页改为全宽响应式网格，中字号同步扩大卡片、缩略图和间距。
- 文本、备注和放大编辑器改用微软雅黑界面字体；备注节点加入独立默认色和完整颜色预设，节点底栏固定在内容上方，文本节点标题栏操作按钮改为更轻量的无底色样式。
- 隐藏 React Flow 品牌标记；工作流空状态、右键菜单、参考图切换、文本滚动列表及亮暗主题细节同步整理。

### 验证

- `npm run build`
- `cargo test --manifest-path src-tauri/Cargo.toml`：50 项测试通过
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`
- `git diff --check`

## 2026-08-09

### 品牌

- 软件用户可见名称统一为 `SuCanvas`，覆盖首页、画布、应用锁、窗口标题、安装包产品名、开发启动提示和本地 API 服务名；现有数据目录及内部兼容标识保持不变。

### ComfyUI 任务与队列

- 支持同一视频生成节点连续提交多个随机种子任务，并在节点取消按钮中显示当前任务数量。
- 取消操作改为每次只取消最早提交的一条任务，优先调用 ComfyUI 按任务 ID 取消接口，并兼容旧版队列/中断接口。
- 在项目首页和画布右上角显示全局 ComfyUI 执行与排队任务总数。
- 将未完成任务、生成参数和客户端 ID 持久化到本地；应用重启后可恢复节点任务数量、排队/执行状态、取消入口和完成结果。
- 恢复任务时重新连接对应的 ComfyUI WebSocket，继续显示当前步骤进度，并在队列切换到下一条任务后同步切换进度来源。
- 清理没有对应持久化任务的陈旧运行状态，避免节点在没有任务时继续显示正在生成。

### 视频生成与预览

- 正常生成、恢复生成和二采预览使用确定性的请求 ID，并在前端按节点与连线 ID 去重，防止同一 ComfyUI 输出被重复创建。
- 视频预览按父生成节点和最后一个已移动预览的位置继续向下排列，保持顶部对齐和紧凑间距。
- 复制视频生成节点时清除运行状态、历史种子和生成结果，同时保留输入素材连接，以便作为新的独立任务排队。
- 固定种子已有生成记录或活动任务时阻止重复提交；随机种子允许连续排队。
- 新建视频生成节点的一采与二采分辨率默认值调整为 0.3 MP 和 0.7 MP。
- 视频生成节点在生成时长同一行新增画面比例下拉框，支持 16:9、9:16、4:3、3:4、2:3、3:2 和 1:1；所选比例会同步写入 ComfyUI 一采、二采尺寸节点，并用于占位框和最终预览尺寸。
- 视频生成节点新增 MiniMax H3 LoRA 选择与权重控制；启动时从 ComfyUI 动态读取 `MinimaxH3` 目录下的可用模型，离线时保留默认模型。
- LoRA 权重支持 0.00 至 2.00、步长 0.05，并在正常生成与二采预览时同步写入工作流的一采、二采 LoRA 节点；后端同时校验模型目录和权重范围。
- 记住最近一次手动选择的 H3 LoRA 和权重，后续新建的视频生成节点自动继承该设置，并在应用重启后继续保留。
- 生成耗时改为读取 ComfyUI `execution_start` 到 `execution_success` 的时间差，排除素材上传和队列等待时间；缺少时间戳时不再显示不准确的估算。
- 二采任务加入与正常生成一致的本地持久化与断链恢复；应用重启后可恢复排队/执行状态、进度、取消入口、二采预览节点及其连线，并分别处理失败、取消和无输出结果。

### 节点与画布交互

- 加粗项目首页“每行项目数”滑块轨道，提高调节控件的可见性。
- 开始执行按钮精简为单独的播放箭头，取消按钮与开始按钮分离，为左侧状态信息留出更多空间。
- 图片输入列表增加“清空”入口，并使用项目统一的二次确认弹窗；清空只断开当前视频节点的图片连接，不删除原始素材。
- 统一确认弹窗中取消与危险操作按钮的字号。
- 支持按住 Ctrl 框选和多选节点，并在多节点拖动结束后保存所有节点位置。
- 支持使用 Ctrl+D 原位复制选中节点；复制与粘贴会读取节点在当前画布中的实际位置，避免使用尚未持久化的旧坐标。
- 支持按住空格从节点、连线或面板区域拖动画布，并在该模式下统一鼠标反馈。
- 加粗已连接边和连接过程中的引导线，提高缩放画布时的辨识度。
- 从 Skill 或本地 API 接收的新文本节点默认放在当前视口中心；中心已被占用时依据节点实时位置向右避让，连续发送也不会相互重叠。
- 文本与备注节点改为输入后自动保存，关联提示词的放大编辑器同步采用自动保存，并移除各处手动保存按钮。
- 视频生成节点中的文本输入区域改为固定占位：无论是否连接提示词都始终保留标题和输入行，未连接时显示“无”，连接或断开文本后节点内部布局不再跳动。
- 二采生成的视频预览节点使用墨绿色框体、顶部 Banner、底部信息条、连接点和选中高亮，和普通视频预览清晰区分。
- 每个视频预览节点新增类似 ComfyUI 的预设色菜单，可在默认色、墨绿、暗红、暗黄和暗蓝之间切换，并持久化框体、顶部 Banner、底部信息条、图标、连接点和选中高亮颜色。
- 文本节点复用视频预览的预设色菜单，可持久化修改框体、标题栏、底部条、图标、连接点和选中高亮颜色；节点标题仅在实际被截断时通过悬浮提示显示完整内容。
- 删除视频节点前新增三选项确认：取消、仅删除节点、同时永久删除本地视频文件；生成视频会使用 ComfyUI 输出映射定位真实文件。
- 视频播放改为全局互斥：任意导入视频或生成预览开始播放时会立即暂停其他视频，并保留各自的播放进度。
- 生成视频预览底部在耗时后新增钢笔图标，可直接复制该视频在数据库生成快照中保存的完整提示词，一采和二采视频均支持复制反馈。
- 新生成且尚未播放的视频会在预览左上角显示 `NEW` 标记，首次开始播放后自动移除并持久化已播放状态；已有历史视频不会被补标。
- 一采和二采任务提交时会立即在最终位置创建与所选画面比例一致的持久化预览占位节点，不再误用参考图片比例。占位节点以低对比蓝黑模糊光团流动展示生成状态和实时进度，完成后原位替换为视频，失败、取消及断链恢复状态也保留在节点中。
- 删除仍在生成中的视频占位节点会先取消其对应的一采或二采任务，取消成功后再移除占位；取消失败时保留节点并显示错误。

### 设置说明

- ComfyUI 输入目录说明明确要求填写 `input` 根目录，程序会自动创建并清理 `infinite-canvas\任务ID` 子目录，避免重复填写缓存目录。
- 应用设置新增 H3 API 工作流文件路径；移动或重命名工作流后可直接更新并持久化，正常生成与二采统一读取该路径，文件缺失时显示具体路径和修复提示。
- 项目新增 `workflows/MiniMax+H3全能参考工作流.json` API 格式工作流，包含当前一采、二采、LoRA、图片、音频与视频输出节点映射，可作为 H3 工作流路径的随项目版本。
- 新增本机应用锁：可在应用设置中启用、修改密码或关闭，密码至少 4 位；启用后每次启动先进入锁屏验证，密码使用随机盐的 Argon2 哈希保存在本机，不保存明文。
- 应用设置新增私密项目管理，可将指定项目持久化标记为私密；项目首页首次使用时默认显示私密项目，关闭“显示私密项目”后才隐藏，并持久化该开关状态以在重启后延续。
- 新增 `Ctrl+H` 快捷键，可快速切换私密项目的显示与隐藏；切换结果与设置开关同步并持久化，文本输入和编辑状态下不会误触；若当前正位于私密项目中，触发快捷键会先保存待处理修改并退出到项目选择页。
- 私密项目管理新增按项目名称即时搜索，并统一设置弹窗及内部列表的滚动条视觉样式。
- 私密项目目录改为两列网格显示，窄窗口下自动回退为单列。
- 应用设置重构为左侧类目导航、右侧具体内容的双栏布局；H3 工作流和两个 ComfyUI 目录归入“基础设置”，私密项目与应用锁分别使用独立类目。
- 新增提示词与生成视频双向关联高亮：提交任务时将提示词节点 ID 固化到每个视频的生成快照，点击生成视频会高亮其原始文本节点，点击提示词会高亮由其生成的全部视频，即使后续断开画布连线仍保持关联。关联计算完全不再使用视频生成节点或画布连线归组，只读取单次提交时捕获的提示词节点 ID；只有提交瞬间标记为 `captured` 的关联永久可信，旧视频的 `verified` 关联会持续核对提示词快照，不一致时立即重新归属或清除，无法唯一确认的记录保持未关联，避免同一生成节点的不同提示词任务互相串联。默认色继续使用原有紫色高亮样式，墨绿、暗红、暗黄和暗蓝则使用更明亮的同色描边与光晕，高亮期间隐藏节点自身实体边框且保持原边框宽度，避免内外两层边框产生色差或挤压节点内容。

### 验证

- `npm run build`
- `cargo test --manifest-path src-tauri/Cargo.toml`：23 项测试通过
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`
- `git diff --check`
